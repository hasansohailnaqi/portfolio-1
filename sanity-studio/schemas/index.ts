import project from './project-schema'
import page from "./page-schema"
import certificate from './certification';

const schemas = [project, page,certificate];
export const schemaTypes=schemas;

export default schemas;
